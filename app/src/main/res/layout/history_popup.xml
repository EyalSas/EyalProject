<?xml version="1.0" encoding="utf-8"?>
<!-- Layout for the Purchase History screen -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white"
    android:padding="16dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Screen title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🧾 Purchase History"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="@color/primary_dark"
        android:layout_marginBottom="16dp" />

    <!-- Scrollable container for purchase history items -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/background_light"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textViewReceipt">

        <!-- Container that holds individual history entries dynamically -->
        <LinearLayout
            android:id="@+id/historyPopupContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:divider="@drawable/divider"
            android:showDividers="middle"
            android:paddingBottom="16dp" />

    </androidx.core.widget.NestedScrollView>

    <!-- Button to close the history screen -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCloseHistory"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:text="Close History"
        android:textColor="@color/white"
        android:backgroundTint="@color/primary_color"
        app:cornerRadius="12dp" />

</LinearLayout>